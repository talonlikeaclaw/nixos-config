[metadata]
name = "bash-history"
description = "Redirected to zsh-history (bash not used)"

[source]
command = "sed '1!G;h;$!d' ${HISTFILE:-${HOME}/.zsh_history}"
display = "{split:;:1..}"
output = "{split:;:1..}"
no_sort = true
frecency = false